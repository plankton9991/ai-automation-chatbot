import React from 'react'
import ReactDOM from 'react-dom/client'
import AIAutomationLanding from './ai-automation-landing'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AIAutomationLanding />
  </React.StrictMode>,
)